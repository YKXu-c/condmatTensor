# PyTorch 2.10 with CUDA 13.0 - Install first using:
# pip install torch --index-url https://download.pytorch.org/whl/cu130
# Note: torch 2.10 is the official version for this project
torch>=2.10
numpy>=1.24,<2.0  # numpy<2.0 required for sober-bo compatibility
matplotlib>=3.7
scipy>=1.10
pyyaml>=6.0

# Optional dependencies for LEVEL 7 (optimization)
# Install these when using BayesianOptimizer:
# pip install sober-bo==2.0.4  # For Bayesian optimization (SOBER backend, preferred)
# pip install botorch>=0.9.0  # For Bayesian optimization (BoTorch backend)
# pip install scikit-optimize>=0.9  # Fallback Bayesian optimization
